[extensions]
exe=
so=.so
tgz=.tar.gz

[paths]
bindir=bin
includedir=include
libdir=lib
prefix=/usr/local
sbindir=sbin

[programs]
ccshared=$(CC) -shared
cxx=c++
ln=ln -f
mkdir=mkdir -m 0755 -p
rm=rm -f
