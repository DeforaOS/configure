ldflags=-ldl -lintl -lossaudio -ltest -lws2_32
dist=Makefile

[target::libtest]
type=libtool
sources=test.c
install=$(LIBDIR)

[target::libs p a c e]
type=libtool
sources=s p a c e.c
install=$(LIBDIR)
